{
  "name": "dugout",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first agent dev workflow harness: Jira ticket → test-first specs → sandboxed execution → fully-linked PRs.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.8"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0"
  }
}
